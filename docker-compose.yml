version: '3.8'

services:
  portfolio-jpstudio:
    build: .
    container_name: portfolio-jpstudio
    restart: always
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
